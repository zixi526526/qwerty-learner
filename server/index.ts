/**
 * Backend scaffold source entrypoint.
 * Runtime currently uses index.cjs so the repo can land the server skeleton
 * before TypeScript server build wiring is added.
 */
export { start } from './index.cjs'
